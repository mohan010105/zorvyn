Fix and stabilize the existing **React Finance Dashboard project** so that it runs correctly when exported from Replit and executed locally.

The application must work properly using:

npm install
npm run dev

No errors should occur during installation or development startup.

Do not remove existing features such as dashboard, transactions, role-based UI, insights, authentication, or charts.

---

PROJECT STRUCTURE FIX

Ensure the project follows a proper **Vite React structure**.

The root directory must contain:

index.html
package.json
vite.config.js
tailwind.config.js
postcss.config.js

Folder structure should look like:

/
index.html
package.json
vite.config.js
tailwind.config.js
postcss.config.js
src/
components/
pages/
context/
utils/
data/
App.jsx
main.jsx

---

INDEX.HTML (IMPORTANT)

Create a proper index.html file in the root folder.

The file must include:

DOCTYPE declaration
Meta viewport tag
Application title
Root mounting div

Example structure:

* HTML document
* Head section with meta charset and viewport
* Title "Finance Dashboard"
* Body containing a div with id="root"
* Script importing /src/main.jsx

This file must allow Vite to mount the React application correctly.

---

MAIN ENTRY FILE

Ensure src/main.jsx exists and correctly renders the app.

It must:

Import React
Import ReactDOM
Import App component
Import global CSS
Mount the application to the root element.

---

PACKAGE.JSON FIX

Ensure package.json includes all required dependencies:

react
react-dom
react-router-dom
recharts
lucide-react
tailwindcss
vite
postcss
autoprefixer

Scripts must include:

"dev": "vite"
"build": "vite build"
"preview": "vite preview"

Running npm install must install all dependencies successfully.

---

TAILWIND CONFIGURATION

Ensure Tailwind is configured correctly:

tailwind.config.js must include:

content paths for:

index.html
src/**/*.{js,jsx}

Include default theme and plugins.

---

ERROR HANDLING

Fix any issues that would cause:

Missing module errors
Broken imports
Incorrect relative paths
Unused or undefined components.

All components should import dependencies correctly.

---

ENVIRONMENT COMPATIBILITY

The application must work when:

Exported from Replit
Cloned from GitHub
Installed locally

Running:

npm install
npm run dev

must start the development server successfully.

---

ADDED FEATURE — AI FINANCIAL INSIGHTS

Enhance the dashboard with an **AI-style financial insights engine**.

This feature must run entirely on the frontend without external APIs.

---

CREATE MODULE

src/utils/aiInsights.js

Functions should analyze transactions and generate:

Highest spending category
Monthly income vs expense comparison
Savings calculation
Next month expense prediction
Financial health score.

---

CREATE COMPONENT

src/components/Insights/AIInsights.jsx

Display insights as cards containing:

Icon
Title
Insight text
Optional small visual indicator.

---

INSIGHTS GENERATED

Examples:

Highest Spending Category

"You spent the most on Food this month."

Monthly Comparison

"Your expenses increased by 14% compared to last month."

Savings

"You saved ₹9,500 this month."

Prediction

"Predicted expenses for next month: ₹17,200."

Financial Health Score

"Financial Health Score: 82/100 — Healthy spending pattern."

---

REAL TIME BEHAVIOR

Whenever transactions are:

Added
Edited
Deleted

All insights must automatically update.

---

DASHBOARD INTEGRATION

Add a new section to the dashboard:

"AI Financial Insights"

Display the top 2 insights.

---

INSIGHTS PAGE

Enhance the Insights page to show:

All generated insights
Supporting charts
Financial health score.

---

EMPTY DATA STATE

If there are not enough transactions:

Display message:

"Add more transactions to unlock AI insights."

---

FINAL VERIFICATION

After fixing the project:

1. npm install must complete successfully
2. npm run dev must start the development server
3. Application must render correctly
4. Charts, transactions, and insights must work
5. No console errors should appear

The final project should behave like a **stable production-ready frontend dashboard application** that runs seamlessly outside Replit.
